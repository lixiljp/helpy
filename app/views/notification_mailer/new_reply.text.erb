<%= t('notify_new_reply', user_name: "#{@user.name} (#{@user.email})", default: "A new customer reply has been received from %{user_name}:") %>

<%= @topic.posts.last.text_formatted_body %>

<%= t('to_reply', default: 'Reply directly to this email or view this online:') %>
<%= admin_topic_url(@topic, host: AppSettings['settings.site_url']) %>
